name: Test
on:
  push:
    branches: [main]
    paths-ignore:
      - "bigearthnet_encoder/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "bigearthnet_encoder/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
